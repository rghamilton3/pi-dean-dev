---
- name: Update apt cache
  ansible.builtin.apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install base packages
  ansible.builtin.apt:
    name: "{{ base_packages }}"
    state: present

- name: Enable + start fail2ban (only if unit exists)
  ansible.builtin.systemd_service:
    name: fail2ban
    state: started
    enabled: true
  when:
    - ansible_facts["service_mgr"] == "systemd"
    - not ansible_check_mode

- name: Enable + start avahi-daemon (mDNS / .local resolution)
  ansible.builtin.systemd_service:
    name: avahi-daemon
    state: started
    enabled: true
  when:
    - ansible_facts["service_mgr"] == "systemd"
    - not ansible_check_mode

- name: Enable weekly fstrim
  ansible.builtin.systemd_service:
    name: fstrim.timer
    state: started
    enabled: true

- name: Monthly NVMe SMART check
  ansible.builtin.cron:
    name: "Monthly NVMe SMART check"
    user: root
    minute: "0"
    hour: "2"
    day: "1"
    job: "smartctl -H /dev/nvme0 || logger -t nvme 'SMART health check failed'"

- name: Reduce swappiness for NVMe longevity
  ansible.posix.sysctl:
    name: vm.swappiness
    value: "10"
    state: present
    reload: true
